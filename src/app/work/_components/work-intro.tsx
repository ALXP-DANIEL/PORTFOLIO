import PageIntro from "@/components/page-intro";

export default function WorkIntro() {
  return (
    <PageIntro
      title="Things I've built"
      description="Products, tools, and experiments — interfaces I designed and shipped, with the occasional weekend rabbit hole. Hover a project to preview it."
    />
  );
}
